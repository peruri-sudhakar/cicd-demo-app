# CI/CD Demo App

Small static website to practice:
- Git + GitHub workflow (branch, PR, merge)
- GitHub Actions CI pipeline
- GitHub Pages deployment

## Run locally
Open `index.html` in browser.

## GitHub Actions
Workflow file is inside:
`.github/workflows/ci.yml`
temp changes
wrong line
